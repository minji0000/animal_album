//모듈로서 사용
export default function TabBar({initialState, onCLick}) {
  this.state = {};
  this.onCLick = onCLick;

  this.template = ()=> {
    
  }

  this.render = () => {

  }

  this.setState = (newState)=> {
    this.state = newState;
    this.render();
  }
}